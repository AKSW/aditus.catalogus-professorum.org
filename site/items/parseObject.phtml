<?php
/*
$ps = get_object_vars($this);

echo "{";

foreach ($ps as $key => $val) {
	//$val = (is_array($val) || is_object($val)) ? "val is array|obje" : $val;
	if ( ! empty($val) && ! is_array($val) && ! is_object($val) ) {
		echo '"' . $key . '":' . $val . ',';
	}	
}

echo "}";
*/
$str = json_encode($this);
echo $str;

?><?php
/* This PHP tag is only to absorb the UNIX-newline */
