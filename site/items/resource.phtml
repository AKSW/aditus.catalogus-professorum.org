<?php /* if ( isset($this->title) && !empty($this->title) ) {
	$title = $this->title;
} else {
	$title = basename($this->resourceUri);	
} */ ?>

<a href="<?= htmlentities($this->resourceUri) ?>"><?= $this->title ?></a><?php
/* This PHP tag is only to absorb the UNIX-newline */
